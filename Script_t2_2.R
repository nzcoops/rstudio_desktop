# Trying something different

library(wakefield)
?wakefield

r_data_frame(
  n = 500,
  race
)


r_data_frame(
  n = 500,
  id,
  Scoring = rnorm,
  Smoker = valid,
  race,
  age,
  sex,
  hour,
  iq,
  height,
  died
)


r_data_frame(
  n = 500,
  race,
  race,
  race
)



r_data_frame(
  n = 500,
  race,
  id,
  race,
  height_cm()
)
